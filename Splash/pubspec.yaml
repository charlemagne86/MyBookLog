name: my_book_log

description: A new Flutter project for book logging.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
